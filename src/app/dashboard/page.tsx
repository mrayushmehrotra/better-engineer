import React from "react";
import CreditsDrawer from "@/components/myComponents/CreditsDrawer";

const page = () => {
  return (
    <div>
      <CreditsDrawer />
    </div>
  );
};

export default page;
